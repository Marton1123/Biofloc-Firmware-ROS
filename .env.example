# ==============================================================================
# Biofloc Firmware ROS - Configuración del Sistema
# ==============================================================================
# INSTRUCCIONES:
# 1. Copia este archivo a .env en la raíz del proyecto: cp .env.example .env
# 2. Completa los valores según tu configuración
# 3. NO subas el archivo .env a git (ya está en .gitignore)
# ==============================================================================

# ---- MongoDB Atlas ----
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/?appName=AppName
MONGODB_DATABASE=SistemasLab
MONGODB_COLLECTION=telemetria
MONGODB_COLLECTION_DEVICES=devices

# ---- Gateway Configuration (Raspberry Pi 3 - Secure IoT Architecture) ----
# IP del gateway en la red interna (hotspot WiFi)
GATEWAY_IP=10.42.0.1

# Interfaz WiFi: wlan0 en Raspberry Pi, wlo1/wlp0s20f3 en NUC/PC
GATEWAY_WIFI_INTERFACE=wlan0

# Credenciales del hotspot WiFi (REEMPLAZAR con tus credenciales)
GATEWAY_WIFI_SSID=<tu-ssid-gateway>
GATEWAY_WIFI_PASSWORD=<tu-password-seguro>

# Red interna del gateway (CIDR notation)
GATEWAY_NETWORK=10.42.0.0/24

# ---- ESP32 Configuration ----
# MAC address del ESP32 (para identificación y verificación)
ESP32_MAC=24:0a:c4:60:c8:e0

# IP típica del ESP32 (asignada por DHCP del gateway)
ESP32_IP=10.42.0.123

# ---- micro-ROS Agent ----
MICROROS_AGENT_IP=10.42.0.1
MICROROS_AGENT_PORT=8888

# ---- ROS 2 Configuration ----
ROS_TOPIC=/biofloc/sensor_data
ROS_NAMESPACE=biofloc

# ---- Sensor Configuration ----
# Location identifier para los datos
SENSOR_LOCATION=tanque_01

# Intervalo de muestreo en milisegundos
SENSOR_SAMPLE_INTERVAL_MS=4000

# ---- Calibración Hardware ----
# Divisor de voltaje (R1=10kΩ, R2=20kΩ)
VOLTAGE_DIVIDER_R1=10000
VOLTAGE_DIVIDER_R2=20000
VOLTAGE_DIVIDER_FACTOR=1500  # (R1+R2)/R2 * 1000

# ---- Calibración pH ----
PH_SLOPE_MILLIPH_PER_VOLT=2559823
PH_OFFSET_MILLIPH=469193

# ---- Calibración Temperatura ----
TEMP_SLOPE=1000000
TEMP_OFFSET_MILLIDEGREES=1382

# ---- Logging ----
LOG_LEVEL=INFO  # DEBUG, INFO, WARN, ERROR
LOG_DATA=true

# ---- Development/Debug ----
# Set to true para modo verbose en scripts
DEBUG_MODE=false
